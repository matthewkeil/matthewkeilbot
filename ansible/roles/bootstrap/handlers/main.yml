---
- name: Restart sshd (bootstrap)
  ansible.builtin.systemd:
    name: ssh
    state: restarted
    enabled: true

- name: Restart ssh socket (bootstrap)
  ansible.builtin.shell: |
    systemctl daemon-reload
    systemctl restart ssh.socket
    systemctl restart ssh
  changed_when: true
